i want to create an open source project to build a hypervisor for macos (and eventually other platforms) which provides the guest a compatibility layer for the GPU API, allowing it to run at native performance on Apple Silicon. 

TL;DR: we want to run a guest OS and have it run at native performance on Apple Silicon, specifically in terms of graphics performance.